<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shedule"
        tools:targetApi="31">

        <!-- Теперь LoginActivity — стартовый экран -->
        <activity
            android:name=".activity.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (студентский экран) -->
        <activity
            android:name=".activity.student.MainActivity"
            android:exported="true" />

        <!-- Экран преподавателя -->
        <activity
            android:name=".activity.teacher.TeacherActivity"
            android:exported="true" />

        <!-- Экран регистрации -->
        <activity
            android:name=".activity.auth.RegisterActivity"
            android:exported="true" />

        <!-- Экран кафедры -->
        <activity
            android:name=".activity.teacher.DepartmentTeachersActivity"
            android:exported="true" />

    </application>
</manifest>
